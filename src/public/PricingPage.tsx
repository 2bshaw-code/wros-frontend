import { Bot, CheckCircle2 } from 'lucide-react'
import { Link } from 'react-router-dom'
import { openBobLite } from './BobLite.tsx'
import { publicPackages } from './packages.ts'

export default function PricingPage() {
  return <section className="mx-auto min-h-[68vh] max-w-6xl px-5 py-20"><p className="text-sm font-bold uppercase tracking-widest text-[#0C8A48]">Pricing packs</p><h1 className="mt-4 max-w-4xl text-4xl font-bold md:text-5xl">Choose the abilities your retail operation needs.</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-gray-600 dark:text-gray-300">Every pack is activated after onboarding review. Selecting a pack does not automatically grant account roles or platform access.</p><div className="mt-10 grid gap-5 md:grid-cols-2 xl:grid-cols-4">{publicPackages.map((plan, index)=><article key={plan.id} className={`flex flex-col border p-6 ${index===1?'border-[#0FA958] bg-[#EAF8EF] dark:bg-[#183328]':'border-[#DDE5E0] bg-white dark:border-[#34444A] dark:bg-[#202C33]'}`}><h2 className="text-2xl font-bold">{plan.name}</h2><p className="mt-2 min-h-16 text-sm leading-6 text-gray-600 dark:text-gray-300">{plan.positioning}</p><div className="mt-6 flex-1 space-y-3">{plan.features.map((feature)=><p key={feature} className="flex items-start gap-2 text-sm"><CheckCircle2 className="mt-0.5 shrink-0 text-[#0FA958]" size={18}/>{feature}</p>)}</div><p className="mt-6 border-t border-[#DDE5E0] pt-4 text-xs font-semibold text-gray-600 dark:border-[#34444A] dark:text-gray-300">{plan.roleOutcome}</p><div className="mt-5 flex flex-wrap gap-2"><Link to={`/apply?package=${plan.id}`} className="rounded-lg bg-[#0FA958] px-4 py-2.5 font-semibold text-white">Apply Now</Link><button type="button" onClick={openBobLite} className="inline-flex items-center gap-2 rounded-lg border border-[#9FB2A6] px-4 py-2.5 font-semibold"><Bot size={17}/>Ask BOB</button></div></article>)}</div></section>
}
