import { useEffect, useState } from 'react'
import Sidebar from '../components/Sidebar.tsx'; import Navbar from '../components/Navbar.tsx'; import Loading from '../components/Loading.tsx'; import axiosClient from '../api/axiosClient'; import type { Customer } from '../types/console'; import { getErrorMessage } from '../utils/helpers'
export default function CRM() { const [customers, setCustomers] = useState<Customer[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); useEffect(() => { axiosClient.get<{ data: Customer[] }>('/customers', { params: { page: 1, limit: 25 } }).then((response) => setCustomers(response.data.data || [])).catch((requestError) => setError(getErrorMessage(requestError, 'Unable to load customers'))).finally(() => setLoading(false)) }, []); return <Shell><div className="space-y-6"><div><p className="text-sm font-semibold text-[#128C7E]">Customer relationships</p><h1 className="mt-1 text-3xl font-bold">CRM</h1></div><div className="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm dark:border-[#263238] dark:bg-[#202C33]">{loading ? <Loading label="Loading customers..." /> : error ? <p className="p-5 text-sm text-red-600">{error}</p> : <table className="w-full text-left text-sm"><thead className="bg-gray-50 text-xs uppercase text-gray-500 dark:bg-[#2A3942] dark:text-gray-300"><tr><th className="px-5 py-3">Name</th><th className="px-5 py-3">Email</th><th className="px-5 py-3">Phone</th></tr></thead><tbody>{customers.map((customer) => <tr key={customer._id} className="border-t border-gray-100 dark:border-[#263238]"><td className="px-5 py-4 font-medium">{customer.name}</td><td className="px-5 py-4 text-gray-500">{customer.email || '—'}</td><td className="px-5 py-4 text-gray-500">{customer.phone || '—'}</td></tr>)}{!customers.length && <tr><td colSpan={3} className="px-5 py-8 text-center text-gray-500">No customers found.</td></tr>}</tbody></table>}</div></div></Shell> }
function Shell({ children }: { children: React.ReactNode }) { return <div className="min-h-screen bg-[#F0F2F5] dark:bg-[#111B21] dark:text-white"><Sidebar /><main className="md:ml-64"><Navbar /><section className="p-5 md:p-8">{children}</section></main></div> }