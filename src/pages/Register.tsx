import { FormEvent, useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { register } from '../api/auth'
import { useAuthStore } from '../state/authStore'
import { getErrorMessage } from '../utils/helpers'
import { AuthFrame } from './Login.tsx'

export default function Register() { const navigate = useNavigate(); const save = useAuthStore((state) => state.login); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [confirm, setConfirm] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const submit = async (event: FormEvent) => { event.preventDefault(); if (password !== confirm) { setError('Passwords do not match'); return } setLoading(true); setError(''); try { const response = await register(email, password); save(response.data.data.token, response.data.data.user); navigate('/dashboard') } catch (requestError) { setError(getErrorMessage(requestError, 'Unable to register')) } finally { setLoading(false) } }; return <AuthFrame title="Create your console" subtitle="Start managing your WROS workspace."><form onSubmit={submit} className="space-y-4">{[['email', email, setEmail], ['password', password, setPassword], ['confirm', confirm, setConfirm]].map(([name, value, setter]) => <label key={name as string} className="block text-sm font-medium">{name === 'confirm' ? 'Confirm password' : (name as string)[0].toUpperCase() + (name as string).slice(1)}<input className="mt-2 w-full rounded-lg border border-gray-300 px-3 py-2.5 dark:border-gray-600 dark:bg-[#2A3942]" type={name === 'email' ? 'email' : 'password'} value={value as string} onChange={(event) => (setter as (value: string) => void)(event.target.value)} required /></label>)}{error && <p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button className="w-full rounded-lg bg-[#0FA958] py-3 font-semibold text-white transition-colors hover:bg-[#0C8A48]" disabled={loading}>{loading ? 'Creating...' : 'Create account'}</button><p className="text-center text-sm text-gray-500">Already registered? <Link className="font-semibold text-[#0FA958]" to="/login">Sign in</Link></p></form></AuthFrame> }