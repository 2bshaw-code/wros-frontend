import { PageShell } from './Dashboard.tsx'; import { useAuthStore } from '../state/authStore'
export default function Settings() { const user = useAuthStore((state) => state.user); const logout = useAuthStore((state) => state.logout); return <PageShell><div className="max-w-2xl space-y-6"><div><p className="text-sm font-semibold text-[#0FA958]">Workspace preferences</p><h1 className="mt-1 text-3xl font-bold">Settings</h1></div><section className="rounded-xl border border-[#EDEDED] bg-white p-6 shadow-sm dark:border-[#263238] dark:bg-[#202C33]"><h2 className="text-lg font-semibold">Profile</h2><dl className="mt-5 space-y-4 text-sm"><div><dt className="text-gray-500">Email</dt><dd className="mt-1 font-medium">{user?.email || 'Not available'}</dd></div><div><dt className="text-gray-500">Role</dt><dd className="mt-1 font-medium">{user?.operatorRole || user?.role || 'Operator'}</dd></div><div><dt className="text-gray-500">Tenant</dt><dd className="mt-1 break-all font-mono text-xs">{user?.tenantId || user?.businessId || 'Not available'}</dd></div></dl><button type="button" onClick={() => { logout(); window.location.assign('/console/login') }} className="mt-6 rounded-lg bg-[#0FA958] px-4 py-2 text-sm font-semibold text-white transition-colors hover:bg-[#0C8A48]">Log out</button></section></div></PageShell> }