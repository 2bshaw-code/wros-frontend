import { ConsoleShell } from '../components/ConsoleShell'
import { useConsoleStore } from '../store/consoleStore'

export default function TypedSettings() { const { user } = useConsoleStore(); return <ConsoleShell><div className="max-w-2xl space-y-6"><div><p className="text-sm font-semibold text-[#128C7E]">Workspace preferences</p><h1 className="mt-1 text-3xl font-bold">Settings</h1></div><section className="rounded-xl border border-gray-200 bg-white p-6 shadow-sm dark:border-[#263238] dark:bg-[#202C33]"><h2 className="text-lg font-semibold">Profile</h2><dl className="mt-5 space-y-4 text-sm"><div><dt className="text-gray-500 dark:text-gray-300">Email</dt><dd className="mt-1 font-medium">{user?.email || 'Not available'}</dd></div><div><dt className="text-gray-500 dark:text-gray-300">Role</dt><dd className="mt-1 font-medium">{user?.operatorRole || user?.role || 'Operator'}</dd></div><div><dt className="text-gray-500 dark:text-gray-300">Tenant</dt><dd className="mt-1 break-all font-mono text-xs">{user?.tenantId || user?.businessId || 'Not available'}</dd></div></dl></section></div></ConsoleShell> }